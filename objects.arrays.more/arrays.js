var myArray = [];
var daysOfTheWeek = ['Sunday', 'Monday', 'Tuesday'];

daysOfTheWeek[1];

myArray = [1, 2, 3, 'String1', 'String2', true, false];

myArray.length;

myArray.push('last');
myArray.pop();

// clear the value but not remove
delete myArray[2];

// remove the value from the array
myArray.splice(2, 1);